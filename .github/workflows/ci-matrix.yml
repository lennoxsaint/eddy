name: CI matrix (3-OS)

# Heavy cross-platform validation: runs the suite + a REAL per-OS render + a built-wheel smoke on
# macOS, Linux, and Windows. Triggered on release tags and on demand (the fast lint/type/test gate
# in ci.yml runs on every push). Requires a remote — wired here, runs once the repo is on GitHub.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  matrix:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # git-describe version

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Eddy requires ffmpeg 8+. macOS/Windows setup-ffmpeg builds (evermeet/gyan) track latest; the
      # Linux build lags, so the Linux leg pulls BtbN's current static build instead.
      - name: Install ffmpeg (macOS/Windows)
        if: runner.os != 'Linux'
        uses: federicocarboni/setup-ffmpeg@v3
      - name: Install ffmpeg 8 (Linux static)
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail
          url=https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-linux64-gpl.tar.xz
          curl -fsSL "$url" -o /tmp/ffmpeg.tar.xz
          mkdir -p /tmp/ffmpeg && tar -xf /tmp/ffmpeg.tar.xz -C /tmp/ffmpeg --strip-components=1
          echo "/tmp/ffmpeg/bin" >> "$GITHUB_PATH"
          /tmp/ffmpeg/bin/ffmpeg -version | head -1

      - name: Install (editable + dev tools)
        run: python -m pip install --upgrade pip && pip install -e ".[dev]"

      - name: Lint + types
        run: |
          ruff check src tests
          mypy src/eddy

      - name: Unit + real-render tests (incl. needs_ffmpeg)
        run: pytest -q --cov=eddy --cov-report=term-missing

      - name: Build wheel + clean-venv smoke
        shell: bash
        run: |
          python -m pip install build
          python -m build --wheel
          VENV="${RUNNER_TEMP:-/tmp}/eddy-smoke"
          python -m venv "$VENV"
          if [ -x "$VENV/bin/python" ]; then PY="$VENV/bin/python"; else PY="$VENV/Scripts/python"; fi
          "$PY" -m pip install --upgrade pip
          "$PY" -m pip install dist/*.whl
          "$PY" -m eddy --version
          "$PY" -m eddy doctor --no-write
