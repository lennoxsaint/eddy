# assets/

Static assets for the composite. Not committed if large (see `.gitignore`).

- `threadify-bg.*` — background for Shorts / Threadify-format outputs (SOP "Add Threadify
  background"). NOT used on the 16:9 long. Source the real brand background before first Shorts run.
- Fonts — the composite/captions resolve system fonts automatically; drop a bold sans here only if a
  specific brand face is required.

The real Threadify logo is the needle-and-thread mark (threadify-needle.png), not the arc+slash icon.
