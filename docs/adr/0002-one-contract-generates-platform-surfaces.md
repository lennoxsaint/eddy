# One contract generates every platform surface

The root Eddy package is canonical. Codex and Claude plugin skill folders are generated projections
with commit and file-hash manifests, while owner installations symlink to the checkout. CI rejects
projection drift because hand-maintained copies previously allowed product doctrine, runtime
instructions, installed caches, and release tags to disagree.

